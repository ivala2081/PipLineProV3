# PipLinePro Environment Configuration
# Copy this file to .env and update the values

# Database Configuration
# SQLite is now the DEFAULT database. Set DATABASE_TYPE=mssql or DATABASE_TYPE=postgresql to use other databases.

# SQLite Database Configuration (DEFAULT - no configuration needed)
DATABASE_TYPE=sqlite

# Alternative: Use full connection URL for any database type
# SQLite: DATABASE_URL=sqlite:///instance/treasury_fresh.db
# MSSQL: DATABASE_URL=mssql+pyodbc://user:password@host:1433/database?driver=ODBC+Driver+17+for+SQL+Server&Encrypt=yes&TrustServerCertificate=no
# PostgreSQL: DATABASE_URL=postgresql://user:password@host:5432/database

# PostgreSQL Database Configuration (Optional - set DATABASE_TYPE=postgresql to use)
# DATABASE_TYPE=postgresql
# POSTGRES_HOST=your-database-host.com
# POSTGRES_PORT=5432
# POSTGRES_DB=your_database_name
# POSTGRES_USER=your_database_user
# POSTGRES_PASSWORD=your_secure_password
# POSTGRES_SSL_MODE=require

# MSSQL Database Configuration (Optional - set DATABASE_TYPE=mssql to use)
# DATABASE_TYPE=mssql
# MSSQL_HOST=your-sql-server-host
# MSSQL_PORT=1433
# MSSQL_DB=your_database_name
# MSSQL_USER=your_sql_server_user
# MSSQL_PASSWORD=your_sql_server_password
# MSSQL_DRIVER=ODBC Driver 17 for SQL Server
# MSSQL_ENCRYPT=yes
# MSSQL_TRUST_SERVER_CERT=no

# Application Configuration
FLASK_ENV=development
DEBUG=True
FLASK_APP=app.py

# SECURITY: Generate a secure secret key for production
# Generate using: python -c "import secrets; print(secrets.token_urlsafe(64))"
# NEVER commit this file with a real SECRET_KEY to version control
SECRET_KEY=dev-secret-key-change-in-production-IMPORTANT-CHANGE-IN-PRODUCTION

# Admin User Configuration (initial user created on first run if INIT_DB=true)
ADMIN_USERNAME=admin
# SECURITY: Use a strong password in production
# This will be required to change on first login if not set
ADMIN_PASSWORD=your-secure-admin-password
ADMIN_EMAIL=admin@pipeline.com

# Note: In production, ALWAYS set ADMIN_PASSWORD in environment variables
# Leaving this unset will generate a random password shown in logs

# Redis Configuration
REDIS_ENABLED=false

# Logging
LOG_LEVEL=INFO

# Database Migration
# Set to true ONLY in development to auto-create database on first run
# In production, use Flask-Migrate or manual SQL migrations
INIT_DB=true

# Flask CLI Configuration
# Required for Flask CLI commands (flask db, flask run, etc.)
FLASK_APP=app.py

# =============================================================================
# PRODUCTION CHECKLIST
# =============================================================================
# Before deploying to production:
# 1. Set SECRET_KEY to a cryptographically random value
# 2. Set FLASK_ENV=production
# 3. Set DEBUG=False
# 4. Configure DATABASE_URL or POSTGRES_* variables
# 5. Set ADMIN_PASSWORD in secure environment variables (NOT in .env file)
# 6. Configure CORS_ORIGINS with your production domain(s)
# 7. Set INIT_DB=false (use migrations instead)
# 8. Enable REDIS for caching: REDIS_ENABLED=true
# 9. Configure SENTRY_DSN for error tracking
# 10. Set up regular database backups
# =============================================================================

# CORS Configuration
# For DEVELOPMENT: Use default React dev server origins (localhost:3000, localhost:5173)
# For PRODUCTION: Specify your frontend domain(s), comma-separated
# Example: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# Error Tracking & Monitoring (Production)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# SENTRY_TRACES_SAMPLE_RATE=0.1
# SENTRY_PROFILES_SAMPLE_RATE=0.1

# Backup Configuration (Optional - for offsite backups)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET=your-backup-bucket-name

# External Services (Optional)
# OPENAI_API_KEY=sk-your-openai-api-key
# SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Blockchain API Keys (Required for Trust Wallet syncing)
# Get free API keys from:
# - Etherscan: https://etherscan.io/apis (for ETH/ERC20 wallets)
# - BSCScan: https://bscscan.com/apis (for BSC/BEP20 wallets)
# - TronScan: Optional - public API works without key (for TRC/TRC20 wallets)
ETHERSCAN_API_KEY=your-etherscan-api-key
BSCSCAN_API_KEY=your-bscscan-api-key
TRONSCAN_API_KEY=your-tronscan-api-key